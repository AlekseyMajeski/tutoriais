name: Download link audit

on:
  push:
    paths:
      - 'impressoras-termicas/**/index.html'
      - 'scripts/audit-download-links.mjs'
      - 'scripts/audit-download-ux.mjs'
      - '.github/workflows/download-audit.yml'
  workflow_dispatch:
  workflow_run:
    workflows:
      - 'Upgrade official download sources'
      - 'Epson legacy driver upgrade'
    types:
      - completed

permissions:
  contents: read

jobs:
  audit:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Classify model driver links
        run: node scripts/audit-download-links.mjs
      - name: Audit download UX across thermal guides
        run: node scripts/audit-download-ux.mjs
